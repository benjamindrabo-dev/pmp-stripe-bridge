{
  "name": "pmp-stripe-google-mcp-bridge",
  "version": "1.2.0",
  "private": true,
  "description": "Pure Majesty Pets Stripe checkout bridge with authenticated Google Ads and GA4 MCP tools on Vercel.",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "mcp-handler": "1.1.0",
    "zod": "^3.25.0"
  }
}
